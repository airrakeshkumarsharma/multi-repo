apiVersion: v1
# Kind of Kubernetes file
kind: Service

# Metadata of service file
metadata:
  name: user-service
spec:
  type: ClusterIP
  ports:
    - protocol:
      port: 8000
      targetPort: 8000
  selector:
    app: user
